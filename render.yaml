##
## [ARCHIVÉ] — Ce fichier n'est plus utilisé. Le déploiement backend se fait sur Railway.
## Voir railway.json et nixpacks.toml à la racine du projet.
##
## Render Blueprint — conservé pour référence historique uniquement.
##

# services:
#   - type: web
#     name: facam-academia-api
#     runtime: node
#     plan: starter
#     autoDeploy: true
#     buildCommand: npm ci && npx turbo run build --filter=api
#     preDeployCommand: npm run prisma:migrate:deploy -w api
#     startCommand: npm run start:prod -w api
#     healthCheckPath: /health
