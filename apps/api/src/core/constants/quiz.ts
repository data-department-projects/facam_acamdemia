/**
 * Constantes liées aux quiz (seuil de réussite, note maximale).
 */

/** Seuil minimal en pourcentage pour valider un quiz chapitre (QCM). */
export const QUIZ_MIN_SCORE_PERCENT = 70;

/** Note maximale du quiz final (sur 20). */
export const QUIZ_FINAL_GRADE_MAX = 20;

/** Note minimale pour délivrer un certificat. */
export const CERTIFICATE_MIN_GRADE = 10;
