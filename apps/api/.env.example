# Base de données (Supabase)
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

PORT=3001
NEXT_PUBLIC_API_URL=http://localhost:3001

JWT_SECRET=cle-secret-facam-dev

# CORS (production : URL du frontend Vercel, ex. https://ton-app.vercel.app)
# Plusieurs origines : séparer par des virgules (sans espaces ou avec)
CORS_ORIGIN=

# SMTP (Nodemailer — Gmail : utiliser un mot de passe d'application)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM_NAME=facam_academia
