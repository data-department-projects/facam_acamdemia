/**
 * Événements côté front pour synchroniser le badge "messages non lus".
 * Utilisé par StudentHeader (affichage badge) et StudentMessagesPage (après mark-read).
 */

export const ANNOUNCEMENTS_READ_EVENT = 'facam_announcements_read';
