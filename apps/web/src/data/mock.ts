/**
 * Fichier de données mock supprimé : l'application s'appuie exclusivement sur l'API backend
 * et la base de données PostgreSQL pour toutes les données affichées.
 *
 * Ce fichier est conservé vide pour éviter les erreurs d'import résiduelles pendant la migration.
 * Toute nouvelle fonctionnalité doit utiliser les services API plutôt que des mocks en mémoire.
 */
