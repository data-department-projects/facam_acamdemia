/**
 * Démo locale du popover (données factices intégrées au composant).
 * Usage : importer dans une page dev ou une story si besoin.
 */

'use client';

import { NotificationPopover } from '@/components/ui/notification-popover';

export function NotificationPopoverDemo() {
  return <NotificationPopover />;
}
